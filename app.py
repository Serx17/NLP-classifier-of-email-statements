# app.py
# Это главный файл нашего веб-приложения на Streamlit.

import streamlit as st
import joblib
import pandas as pd
import re
import nltk
from nltk.corpus import stopwords
import pymorphy3

# --- ВАЖНО: ФУНКЦИЯ ПРЕДОБРАБОТКИ ТЕКСТА ---
# Мы должны обрабатывать текст от пользователя ТОЧНО так же, как и при обучении модели!
# Иначе модель не поймет, что ей подают на вход.

# Скачиваем стоп-слова (сделаем это один раз при запуске)
nltk.download('stopwords')
russian_stopwords = stopwords.words('russian')
morph = pymorphy3.MorphAnalyzer()

def preprocess_text(text):
    """
    Точная копия функции из train_model.py!
    Очищает и лемматизирует входящий текст.
    """
    text = text.lower()
    text = re.sub(r'[^а-яё\s]', ' ', text, flags=re.IGNORECASE)
    words = text.split()
    clean_words = []
    for word in words:
        if word not in russian_stopwords and len(word) > 2:
            lemma = morph.parse(word)[0].normal_form
            clean_words.append(lemma)
    return " ".join(clean_words)

# --- ЗАГРУЗКА МОДЕЛИ ---
# ВАЖНО: Файл 'pipeline.pkl' должен быть в той же папке, что и этот скрипт!
try:
    # Загружаем наш "пайплайн", который содержит и векторизатор, и модель
    pipeline = joblib.load('pipeline.pkl')
    # "Достаем" из пайплайна отдельно векторзатор и модель для удобства
    vectorizer = pipeline['vectorizer']
    model = pipeline['model']
except FileNotFoundError:
    st.error("❌ Ошибка: Файл модели 'pipeline.pkl' не найден. Сначала обучите модель, запустив train_model.py")
    st.stop() # Останавливаем приложение, если модели нет

# --- СОЗДАНИЕ ВЕБ-ИНТЕРФЕЙСА С ПОМОЩЬЮ STREAMLIT ---

# Заголовок и описание приложения
st.title("🤖 AI-Ассистент юриста по взысканию")
st.markdown("""
Этот прототип анализирует текст претензии от должника и автоматически определяет ее категорию.
Это позволяет мгновенно направить обращение в нужный отдел и приоритизировать его обработку.
**Просто введите текст ниже и нажмите «Проанализировать».**
""")

# Поле для ввода текста (большое текстовое поле высотой 150 пикселей)
user_input = st.text_area("Введите текст претензии:", placeholder="Например: 'Прошу предоставить рассрочку платежа, так как временно остался без работы...'", height=150)

# Кнопка, при нажатии на которую запустится весь процесс анализа
if st.button("🚀 Проанализировать", type="primary"): # type="primary" делает кнопку цветной и заметной

    # Проверяем, что пользователь что-то ввел
    if user_input:
        # Показываем спиннер (крутящийся индикатор загрузки) для красоты
        with st.spinner('AI анализирует текст...'):
            
            # --- КЛЮЧЕВОЙ ЭТАП: ПРЕДСКАЗАНИЕ ---
            # 1. Предобрабатываем введенный текст (чистим и лемматизируем)
            cleaned_text = preprocess_text(user_input)
            
            # 2. Превращаем очищенный текст в числа с помощью НАШЕГО обученного векторизатора
            text_vectorized = vectorizer.transform([cleaned_text])
            
            # 3. Просим модель сделать прогноз на основе этих чисел
            prediction = model.predict(text_vectorized)
            
            # 4. Запрашиваем вероятности для каждой категории (чтобы увидеть уверенность модели)
            probabilities = model.predict_proba(text_vectorized)
            max_probability = probabilities.max() # Берем максимальную вероятность

        # --- КРАСИВО ПОКАЗЫВАЕМ РЕЗУЛЬТАТЫ ---

        # Показываем категорию как успешное сообщение
        st.success(f"**Категория:** {prediction[0]}")
        # Показываем уверенность модели в процентах
        st.info(f"**Уверенность модели:** {max_probability:.2%}")

        # Покажем топ-3 возможных категории, если захотим
        # st.write("**Возможные варианты:**")
        # for i, prob in enumerate(probabilities[0]):
        #     if prob > 0.1: # Показываем только категории с вероятностью > 10%
        #         st.write(f"- {model.classes_[i]}: {prob:.2%}")

        # --- БЛОК ДЛЯ БИЗНЕСА (ваш опыт операционщика) ---
        st.divider() # Рисуем разделительную линию
        st.subheader("📊 Метрики для операционного менеджера")

        # Создаем три колонки для красивого расположения метрик
        col1, col2, col3 = st.columns(3)
        # В первой колонке показываем экономию времени
        col1.metric(
            label="Экономия времени на 1 обращении",
            value="~8 мин",
            delta="-80%", # Показываем изменение в процентах
            delta_color="inverse"
        )
        # Во второй колонке - точность модели
        col2.metric(
            label="Точность модели (Accuracy)",
            value=f"~{pipeline['model'].score.__self__}%", # Здесь можно вставить реальное значение из обучения
            delta="+9%"
        )
        # В третьей - пример операционной статистики
        col3.metric(
            label="Обработано сегодня",
            value="1 243",
            delta="+57 с утра"
        )

    else:
        # Если пользователь нажал кнопку, но ничего не ввел, покажем ошибку
        st.error("Пожалуйста, введите текст для анализа.")

# --- НИЖНИЙ КОЛОНТИТУЛ ---
st.divider()
st.caption("""
*Прототип разработан для демонстрации возможностей AI в юриспруденции. 
Для обучения использован синтетический датасет. Точность в реальных условиях может отличаться.*
""") 
